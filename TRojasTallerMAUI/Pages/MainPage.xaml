<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TRojasTallerMAUI.Pages.MainPage"
             Title="GridLayout">
    <Grid RowDefinitions="*,*" ColumnDefinitions="*,*">
        <BoxView BackgroundColor="Red" Grid.Row="0" Grid.Column="0"/>
        <BoxView BackgroundColor="Green" Grid.Row="0" Grid.Column="1"/>
        <BoxView BackgroundColor="Blue" Grid.Row="1" Grid.Column="0"/>
        <StackLayout Grid.Row="1" Grid.Column="1">
            <BoxView BackgroundColor="Yellow" HeightRequest="100"/>
            <Button Text="Ir a StackLayout" Clicked="OnGoToStackClicked"/>
        </StackLayout>
    </Grid>
</ContentPage>
